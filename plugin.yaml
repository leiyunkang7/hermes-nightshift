name: nightshift
version: "0.1.0"
description: "Delegate one task and return its summary (Week 1 of hermes-nightshift)."
author: "leiyunkang7"
license: "MIT"

# Entry point — Hermes calls register(ctx) on plugin load
entry: "__init__:register"

# Slash commands this plugin adds
commands:
  - name: nightshift
    description: "Delegate one task and return its summary."
  - name: nightshift-status
    description: "Show status of running nightshift tasks (Week 2)."
